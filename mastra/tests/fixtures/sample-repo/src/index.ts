/**
 * Sample Repository - Entry Point
 * Used for testing RAG indexing
 */

export function main(): void {
  console.log("Hello from sample repo");
}

export function calculateSum(a: number, b: number): number {
  return a + b;
}

export interface User {
  id: string;
  name: string;
  email: string;
}
